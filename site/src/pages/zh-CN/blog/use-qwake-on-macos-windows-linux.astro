---
import BlogLayout from "../../../layouts/BlogLayout.astro";
import { SITE_URL } from "../../../data/site";

const title = "如何在 macOS、Linux、Windows 上使用 Qwake";
const description =
  "一篇实用的 Qwake 跨平台使用指南，覆盖 macOS LaunchAgent、Linux systemd 与 crontab fallback、Windows Task Scheduler。";
const jsonLd = [
  {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline: title,
    description,
    author: { "@type": "Organization", name: "Qwake" },
    mainEntityOfPage: `${SITE_URL}/zh-CN/blog/use-qwake-on-macos-windows-linux/`,
    datePublished: "2026-05-30T00:00:00+08:00",
    dateModified: "2026-06-20T00:00:00+08:00",
    image: `${SITE_URL}/og-image.svg`,
    publisher: {
      "@type": "Organization",
      name: "Qwake",
      url: SITE_URL
    }
  },
  {
    "@context": "https://schema.org",
    "@type": "HowTo",
    name: title,
    step: [
      { "@type": "HowToStep", text: "全局安装 Qwake。" },
      { "@type": "HowToStep", text: "确认本机 AI 编码 CLI 可用。" },
      { "@type": "HowToStep", text: "为目标 provider 和时间安装调度任务。" },
      { "@type": "HowToStep", text: "通过 status 和 logs 验证结果。" }
    ]
  }
];
---

<BlogLayout
  locale="zh-CN"
  title={title}
  description={description}
  canonicalPath="/zh-CN/blog/use-qwake-on-macos-windows-linux/"
  alternatePath="/blog/use-qwake-on-macos-windows-linux/"
  jsonLd={jsonLd}
>
  <h2>Qwake 能保证什么，不能保证什么</h2>
  <p>
    Qwake 会通过你本机已经可用的 CLI 发送极小的 wake 请求。它可以验证定时任务是否真的执行过，
    以及 provider 是否接受了请求；但如果机器关机、完全深度睡眠、离线，或者 provider 拒绝请求，
    它不能保证额度一定刷新。
  </p>

  <h2>安装 Qwake</h2>
  <pre><code>npm install -g @sysiphus/qwake
qwake init
qwake doctor
qwake wake mock</code></pre>

  <h2>跨平台统一命令</h2>
  <p>
    Qwake 在 macOS、Linux、Windows 上都保持同一套调度命令：
  </p>
  <pre><code>qwake schedule install codex claude --times 06:05,11:10,16:15,21:20
qwake schedule status
qwake schedule doctor
qwake schedule test codex claude
qwake schedule repair codex claude
qwake schedule logs
qwake schedule uninstall codex claude</code></pre>

  <h2>macOS：LaunchAgent</h2>
  <p>
    在 macOS 上，Qwake 会安装 LaunchAgent。对笔记本工作流来说，这通常比 cron 更合适，
    因为 launchd 和用户会话集成得更好。定时任务默认启用 smart skipping：只有当上一次成功 wake
    已经超过配置的 5 小时窗口加 buffer 后，Qwake 才会再次真实调用 provider。
  </p>
  <pre><code>launchctl list | rg qwake
cat ~/Library/LaunchAgents/com.qwake.claude.plist</code></pre>

  <h2>Linux：优先 systemd，兜底 crontab</h2>
  <p>
    在 Linux 上，Qwake 会先尝试安装 <code>systemd --user</code> service 和 timer。
    如果机器没有可用的 systemd user service，Qwake 会自动 fallback 到 <code>crontab</code>，
    并安装等价的每日定时条目。
  </p>
  <p>如果使用 systemd，可用下面命令查看：</p>
  <pre><code>systemctl --user status qwake-claude.timer
journalctl --user -u qwake-claude.service</code></pre>
  <p>如果 fallback 到 cron，可用下面命令查看：</p>
  <pre><code>crontab -l | grep QWAKE:claude</code></pre>

  <h2>Windows：Task Scheduler</h2>
  <p>
    在 Windows 上，Qwake 会通过 <code>schtasks</code> 安装每日任务。同一条 schedule 命令会为每个时间点
    创建一个任务，并把日志写到 <code>~/.qwake/logs</code>。
  </p>
  <pre><code>where qwake
qwake schedule install codex claude --times 06:05,11:10,16:15,21:20
qwake schedule status
schtasks /Query /TN "\Qwake\claude-0605" /V /FO LIST</code></pre>

  <h2>排障</h2>
  <p>如果定时唤醒没有执行，先看这些：</p>
  <pre><code>qwake schedule status
qwake schedule doctor
qwake schedule test codex claude
qwake schedule logs
qwake doctor</code></pre>
  <p>
    三个平台的日志语义是统一的：<code>success</code>、<code>failed</code>、
    <code>limited</code>、<code>skipped</code>。
  </p>
</BlogLayout>
